import { createNotification } from '@/lib/appwrite/api'
import { useEffect } from 'react'

const SendNotifcation = ({userId ,friend, Title, desc}:any) => {
    console.log("Called Send Notification")
   useEffect(()=>{
    
   },[])

  return (
    <></>
  )
}

export default SendNotifcation